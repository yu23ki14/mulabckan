# OpenAI API key (server-side only). Required.
OPENAI_API_KEY=

# OpenAI model (optional). Defaults to gpt-4o-mini.
# OPENAI_MODEL=gpt-4o-mini

# CKAN endpoint (optional). Defaults to 西粟倉村.
# CKAN_BASE_URL=https://ckan.nishiawakura-mulabo.jp/api/3/action
